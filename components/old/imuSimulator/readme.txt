	  imuSimulator.  Real-time simulation of the imu.
	  This pretty much just takes the output of the plane simulator,
	  pulls out the accelerations corresponding to the imu measurements,
	  and outputs the accelerations and angular velocities in the plane
	  coordinate system.

  
