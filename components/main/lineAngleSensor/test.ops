#!/usr/bin/env deployer

import("lineAngleSensor")
import("soem_master")
import("soem_ebox")
 
loadComponent("soemMaster","soem_master::SoemMasterComponent")
loadComponent("lineAngleSensor","LineAngleSensor")

# XXX Update this script for correct operation
 
var double base_hz = 100;
setActivity("lineAngleSensor",1.0/base_hz,HighestPriority,ORO_SCHED_RT)
setActivity("soemMaster",.005,HighestPriority,ORO_SCHED_RT)
soemMaster.ifname = "eth1"

#connectPeers("lineAngleSensor","soemMaster")

soemMaster.configure()
soemMaster.start()

lineAngleSensor.configure()
lineAngleSensor.start()
