ifdef CAROUSEL1
ifdef PC

include_rules

agdir = autogenerated
: foreach IndoorsCarouselSimulator.cpp | $(agdir)/acado_common.h $(agdir)/sim_configuration.h |> !cxx_orocos |>
: *.o $(agdir)/integrator.o |> !ld_orocos |> libIndoorsCarouselSimulator.so
endif
endif
