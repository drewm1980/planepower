# This assumes you have raseome checked out at the same level as planepower!
CPPFLAGS += -I../../../../rawesome/carousel-cpp/
include_rules
: foreach *.cpp |> !cxx_orocos |>
: *.o |> !ld_orocos |> %d.so
