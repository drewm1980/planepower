include_rules

# make rawesome export the solver
propsdir = $(PLANEPOWER_ROOT)/properties
export PYTHONPATH
genfile = $(PLANEPOWER_ROOT)/rawesome_stuff/generateMhe.py
: $(genfile) |> python -B $(genfile) $(propsdir) |> acado_common.h ocp.o solver.hpp whereami.txt mhe_weights.h mhe_steady_state.h
