include_rules

# make rawesome export the solver
propsdir = $(PLANEPOWER_ROOT)/properties
export PYTHONPATH
genfile = $(PLANEPOWER_ROOT)/rawesome_stuff/generateMhe.py
: $(genfile) |> python -B $(genfile) $(propsdir) |> acado_common.h ocp.so solver.hpp mhe_structs.h

#EXPORTED_CODE = $(PLANEPOWER_ROOT)/acado_codegen/mhe/exported_code
#CPPFLAGS_OROCOS += -I$(EXPORTED_CODE)
#CPPFLAGS_OROCOS += -I $(PLANEPOWER_ROOT)/acado_codegen/
#LDFLAGS_OROCOS += -L$(PLANEPOWER_ROOT)/acado_codegen -lmhe_solver

#: dynamicMHE.cpp | acado_common.h ocp.so |> !cxx_orocos |>
#: *.o | $(PLANEPOWER_ROOT)/acado_codegen/libmhe_solver.a  |> !ld_orocos |> dynamicMHE.so

