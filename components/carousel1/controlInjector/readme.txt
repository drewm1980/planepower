This component lets you set the flight surface controls from some non-real
time source, i.e. lua, and have them applied the next time the component is
triggered.  This is useful if you want to run an experiment where the
control changes are synced with the camera trigger.
