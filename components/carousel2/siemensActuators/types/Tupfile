ifdef CAROUSEL2

include_rules

CODEGEN_SOURCE_DIRECTORY=../../siemensDrives
CODEGEN_SOURCE_HEADER=$(CODEGEN_SOURCE_DIRECTORY)/SiemensDriveCommand.h
CODEGEN_SOURCE_STRUCT=SiemensDriveCommand

include ../../codegen_glue.tup

endif
