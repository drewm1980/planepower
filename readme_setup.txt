#!/usr/bin/env bash

# Steps for getting the carousel code running for a new developer:

# sudo adduser bob

cd
echo "Clone the repository from somewhere"
git clone ~planepower/Work/SVN/PLANEPOWER/TRUNK/orocos planepower

cp ~planepower/.gitconfig ~

