#ifndef TIME_HIGHWIND_H
#define TIME_HIGHWIND_H

// This is a workaround for C still not having C++ style struct syntax,
// and not typedeffing stuff for convenience in the system headers
typedef struct timeval timeval;

#endif
